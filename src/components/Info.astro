---
import { LUMNA_PICTURE, SEPARATOR, DESCRIPTION } from "../constants";
import SectionTitle from "./SectionTitle.astro";
---

<section class="flex flex-col md:flex-row mb-36 max-w-screen-xl mx-auto">
	<div class="flex-1 relative">
		<img
			id="imagen"
			class="w-full mix-blend-exclusion brightness-50"
			decoding="async"
			src={LUMNA_PICTURE}
			alt="Imagen de Lumna"
		/>
		<img
			alt="separator"
			decoding="async"
			src={SEPARATOR}
			class="absolute rotate-90 -translate-x-64 translate-y-[37%] md:translate-x-2 md:translate-y-0 md:rotate-0 bottom-0 right-0 h-full"
		/>
	</div>
	<div class="flex-1 flex flex-col mt-10 px-4 sm:px-10 text-center">
		<SectionTitle>
			Hola soy Lumna <span slot="icon" class="text-4xl">👋</span>
		</SectionTitle>

		<p class="text-white/50 text-xl">
			{DESCRIPTION}
		</p>
	</div>
</section>
