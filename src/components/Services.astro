---
import { SERVICES } from "../constants";
import Card from "./Card.astro";
import SectionTitle from "./SectionTitle.astro";
---

<section class="relative max-w-screen-lg mx-auto px-4 mb-10 md:mb-36">
	<SectionTitle>Servicios</SectionTitle>
	<ul class="flex flex-wrap gap-6 justify-around">
		{
			SERVICES.map((service) => (
				<Card
					name={service.name}
					desc={service.desc}
					img={service.img}
				/>
			))
		}
	</ul>

	<div
		class="absolute rounded-full top-1/3 left-2/4 w-32 h-32 bg-lumna-400/50 blur-3xl"
	>
	</div>
	<div
		class="absolute rounded-full top-2/3 left-2/3 w-32 h-32 bg-lumna-600/50 blur-3xl"
	>
	</div>
	<div
		class="absolute rounded-full top-3/4 left-1/3 w-32 h-32 bg-lumna-400/50 blur-3xl"
	>
	</div>
</section>
